# This is the folder consisting of all the example data


See `generation/helpers.py` for generation helpers

See `generation/standard.py` for a standardized generation script

See `generation/dst.yaml` for an example config file for generation

See `corruption/helpers.py` for corruption helpers