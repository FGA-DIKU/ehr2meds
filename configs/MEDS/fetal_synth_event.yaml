subject_id_col: subject_id

patients_info:
  dob:
    code: "DOB"
    time: $Birthdate as "%Y-%m-%d %H:%M:%S%.f"
  dod:
    code: "DOD"
    time: $Deathdate as "%Y-%m-%d %H:%M:%S%.f"
  gender:
    code: f"GENDER//{$Gender}"
    time: null
    
diagnosis:
  diagnosis:
    code: f"D//{$Diagnosekode}"
    time: $timestamp as "%Y-%m-%d %H:%M:%S%.f"

procedure:
  procedure:
    code: f"P//{$ProcedureCode}"
    time: $timestamp as "%Y-%m-%d %H:%M:%S%.f"

labtest:
  labtest:
    code: f"LAB//{$BestOrd}"
    time: $timestamp as "%Y-%m-%d %H:%M:%S%.f"
    numeric_value: '(extract group 1 of /^\s*([-+]?(?:\d+(?:\.\d*)?|\.\d+)(?:[eE][-+]?\d+)?)\s*$/ from $samplevalue) as float64'  # Regex to extract numeric value from string, allowing for scientific notation and whitespace
    text_value: "$samplevalue" # the value as string, for cases where the value is not numeric (e.g., "NEG", "POS", "<0.1", etc.). Duplicates where text and numeric are the same are dropped.

admissions:
  IN:
    code: f"IN//{$Afsnit}"
    time: $timestamp_in as "%Y-%m-%d %H:%M:%S%.f"
  OUT:
    code: f"OUT//{$Afsnit}"
    time: $timestamp_out as "%Y-%m-%d %H:%M:%S%.f"
